import axios from "axios";

// const api = axios.create({
//     baseURL: "http://localhost:8080",
// });

const api = axios.create({
    baseURL: "https://sha-backend-y0r0.onrender.com",
});

export default api;